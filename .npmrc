# Use pnpm for package management
package-manager=pnpm

# Store packages in node_modules (compatible with Next.js)
node-linker=hoisted

# Enable strict peer dependencies
strict-peer-dependencies=false

# Auto install peers
auto-install-peers=true
